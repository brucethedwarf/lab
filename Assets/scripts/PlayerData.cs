using UnityEngine;

public class PlayerData : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public class Playerdata 
    {
        public int betterjbob;
        public int money;
        public float[] position;
    }
}
